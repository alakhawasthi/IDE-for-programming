// Global Variables
var run = document.getElementsByClassName("compile")[0]

// Main Logic
run.addEventListener("click", function (e) {
    let text = document.getElementsByClassName("code")[0].value
    
})
